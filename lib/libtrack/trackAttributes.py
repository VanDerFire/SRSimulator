class track:

    def __init__(self, eventName, trackName, trackDistance, laps, avgTime, avgSpeed):

        self.eventName = eventName

        self.trackName = trackName

        self.trackDistance = trackDistance

        self.laps = laps

        self.avgTime = avgTime

        self.avgSpeed = avgSpeed